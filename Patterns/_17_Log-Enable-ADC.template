Pattern(myPat){     
    cycle=low,  r0=0;
    cycle=low,  r0+=1, log(ADC);
    cycle=low,  r0+=1, log(ADC);
    cycle=low,  r0+=1, log(ADC);
    cycle=low,  r0+=1, log(ADC);    
    cycle=high, r0+=1, log(ADC);
    cycle=high, r0+=1, log(ADC);
    cycle=high, r0+=1, log(ADC);
    cycle=high, r0+=1, log(ADC);
    cycle=high, r0+=1, log(ADC);
